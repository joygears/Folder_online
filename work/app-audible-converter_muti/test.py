import dis


def test():
    if 1:
        for i in range(m):
            split_points.append([i * dur_per_one, (1 + i) * dur_per_one])
            if n is True:
                split_points.append([duration - n, duration])
    else:
        pass
dis.dis(test)